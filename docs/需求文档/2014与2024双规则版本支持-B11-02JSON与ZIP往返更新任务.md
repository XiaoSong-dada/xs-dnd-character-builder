# B11-02 JSON 与 ZIP 完整包往返 · 更新任务

- 对应 TODO：B11-02（[TODO 计划](2014与2024双规则版本支持-TODO计划.md)）。
- 实现日期：2026-09-16。
- 状态：已完成（服务层无需改动，补齐 2024 覆盖）。

## 1. 现状与范围

- `character-json.ts`：`exportDraft` 剥离 `media` 后序列化完整草稿；`importDraft` 支持 schema v2—v8，未知/缺失版本报错，`schemaVersion < 8` 的 2024 文件按既定"旧版 2024 草稿"错误处理（隔离政策不变）。
- `character-package.ts`：ZIP 内含 `character.json` 与媒体文件，导入重建角色与媒体 ID。
- 两个服务均无 2014 硬编码；本批以 2024 覆盖测试为主。

## 2. 验证

- `test/services/character-json.test.ts` 新增 1 项：v8 2024 草稿（职业／子职、圣职选择、装备、法术选择、语言、`backgroundAbilityAllocation`、媒体）导出→导入字段等价，且导出的 JSON 不含媒体。
- `test/services/character-package.test.ts` 新增 1 项：2024 草稿含头像时 ZIP 往返保留规则版本与媒体内容。
- 既有 v2—v7 迁移、旧版 2024 拦截、无图片往返用例保持通过。

## 3. 边界

- 不改 schema、不改隔离政策；`drafts:unsupported:v1` 隔离草稿的原始导出由 B11-05 覆盖。
