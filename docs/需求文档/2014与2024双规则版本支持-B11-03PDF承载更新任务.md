# B11-03 PDF 角色卡承载适配 · 更新任务

- 对应 TODO：B11-03（[TODO 计划](2014与2024双规则版本支持-TODO计划.md)）。
- 实现日期：2026-09-16。
- 状态：已完成（沿用既有三页中文 AcroForm 模板，不新增模板字段）。

## 1. 范围与设计（`app/src/services/export-pdf.ts`）

- `layoutFeatures` 改为 `layoutFeatureEntries(entries)` 接收文本条目：在原有特性/专长文本之后追加 `formatExportResources(model.resources)` 生成的"资源："行（含上限、单位与恢复时机／短休固定恢复数），继续走原有两段字段容量与"另有 N 项省略"诊断。
- 2024 选择类特性（武器精通、祈唤、圣职、子职选项）、物种特性与起源专长由 B11-01 的模型直接进入同一特性文本区；法术表、物品与攻击沿用既有字段。
- 长文本超限仍产生 `content-truncated` 警告，不阻断导出。

## 2. 验证

- `test/services/export-pdf.test.ts` 新增 1 项：2024 游侠样例（武器精通＋猎人猎物＋资源）填充三页模板，无 error 诊断、表单扁平化、嵌入单一 Noto Sans SC Regular 子集。
- `test/fixtures/pdf-font.ts` 的字体语料新增 `build-export-data.ts`，新中文文案进入 `pdf-font.test.ts` 覆盖；新增字符均为 GB2312 常用字，字体子集无需重建。
- 既有 2014 黄金样例、法师样例、字体与技能行测试保持通过。

## 3. 边界

- 不引入 PDF 模板结构改动（不新增字段）；资源与选择类内容并入现有"特性与专长"文本区。
