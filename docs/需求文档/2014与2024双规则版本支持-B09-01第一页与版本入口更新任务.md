# B09-01 第一页与版本入口 · 更新任务

- 对应 TODO：B09-01（[B09 更新计划](2014与2024双规则版本支持-B09更新计划.md) 第 4.1 节）。
- 实现日期：2026-09-15。
- 状态：已完成（2026-09-15）。
- 已接受的产品默认：Q-B09-1 采用第一页内联提示（不弹窗打断）；Q-B09-5 2024 来源步骤不额外提供"只用核心"按钮（B09-02 处理）。

## 1. 目标与范围

- 目标：第一页可显式选择规则版本，并按 B00-02 落实"首次默认 2014、记住本设备明确选择、未开放版本回退并说明、已有角色按自身版本打开"；开放 2024 车卡入口。
- 本次范围：
  - `SetupStep` 增加"规则版本"选择（顺序：版本 → 目标等级 → 属性生成方式），书目仍为第 2 步。
  - 新建草稿按本设备偏好解析版本；偏好未开放时回退 2014 并给出内联说明。
  - `OPEN_RULESETS` 纳入 `5e-2024`；导入门禁文案改为通用表述。
  - `StartPanel` 版本文案动态化，并在角色条显示草稿版本；旧 2024 只读隔离区保持只读与"不自动转换"说明。
  - 已有构筑选择的草稿锁定版本卡片（不在原地转换；另建流程归 B09-03）。
- 不包含：第 2 步来源步骤改造、无施法跳步（B09-02）；改版另建与影响提示（B09-03）；角色卡文案与提示级校验呈现（B09-04）；跑团与导出（B10、B11）。

## 2. 共享能力与文件

| 文件 | 改动 |
| --- | --- |
| `src/services/ruleset-preference.ts` | 新增：本设备版本偏好读写（localStorage、SSR 安全、容错）与 `resolveInitialRuleset`（记忆版本未开放时回退并报告） |
| `src/rules/repositories.ts` | `OPEN_RULESETS` 纳入 `5e-2024`，更新注释 |
| `src/rules/draft-progress.ts` | 新增：`hasBuildChoices(draft)` 判定是否存在构筑选择 |
| `src/stores/character-drafts.ts` | `createDraft()` 按偏好解析版本；未开放版本提示改为通用文案 |
| `src/views/character-builder/components/SetupStep.vue` | 版本卡片、锁定态与回退说明 |
| `src/views/character-builder/components/StartPanel.vue` | hero 版本文案与角色条版本标签 |
| `src/views/character-builder/hooks/useCharacterBuilderPage.ts` | `defaultRuleset`、回退提示、`updateRuleset`（写偏好） |
| `src/views/character-builder/index.vue` | 接线（props／事件／锁定态） |

## 3. 实施步骤

1. 先写类型与服务测试（偏好读写、回退解析），再实现服务。
2. 规则层开放 2024 并同步矩阵复核测试的入口断言（保留"未开放版本仍拒绝"的覆盖）。
3. 数据流：store 解析 → hook 暴露默认版本与回退提示 → 第一页组件渲染与事件。
4. 界面：第一页版本卡片、锁定说明、回退提示；起点页版本文案与角色条标签。
5. 全量验证与文档同步。

## 4. 验证

- 新增 `test/services/ruleset-preference.test.ts`：读写、缺省、损坏数据、无 `localStorage`、回退与不开放判定。
- 扩展 `test/components/SetupStep.test.ts`：版本卡片渲染与事件、锁定态与说明、回退提示。
- 扩展 `test/stores/character-drafts.test.ts`：按偏好创建、2024 导入成功、未开放版本仍拒绝（以无效版本值覆盖）。
- 更新 `test/rules/b08-matrix-review-2024.test.ts`：入口断言改为"2024 已开放、未知版本仍拒绝"，矩阵闭合断言不变。
- 扩展 `test/components/StartPanel.test.ts`：默认版本 hero 文案与角色条版本标签。
- 门槛：`vue-tsc -b`、`vitest run`、`vite-ssg build`、`git diff --check`；新增中文文本后重建 PDF 字体子集。

## 5. 文档同步

`docs/frontend-architecture.md`（偏好服务与第一页数据流）、`docs/rules.md`（入口开放范围与边界）、`docs/dnd-classes.md`（完成度行）、TODO 计划（B09-01 勾选）、B09 更新计划（批次状态）。

## 6. 完成标准

- 新建 2024 草稿成功，刷新后仍为 2024；偏好记忆与未开放回退有测试。
- 2014 既有创建、打开、导入导出行为不变。
- 已有构筑的草稿不能原地改版本（卡片锁定并说明）。
- 门槛全部通过且文档同步完成。

## 7. 结果回填

- 变更：新增版本偏好服务与 `hasBuildChoices`；`OPEN_RULESETS` 纳入 2024；第一页版本卡片（含锁定态与回退提示）；起点页版本文案与角色条版本标签；store 按偏好创建；门禁文案通用化。
- 检查结果：`vue-tsc -b` 通过；`vitest run` 119 文件／988 项全部通过；`vite-ssg build` 通过；`git diff --check` 无空白错误；PDF 字体子集已重建。
- 剩余问题：改版"另建"流程与影响提示归 B09-03；来源步骤 2024 渲染与跳步归 B09-02。
- 可勾选 TODO：B09-01。
