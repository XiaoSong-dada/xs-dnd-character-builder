# B09-05 端到端验收（2024 战士／法师＋2014 回归） · 更新任务

- 对应 TODO：B09-05（[B09 更新计划](2014与2024双规则版本支持-B09更新计划.md) 第 4.5 节）。
- 实现日期：2026-09-15。
- 状态：已完成；B09 阶段收尾。

## 1. 目标与范围

- 目标：2024 角色可从创建走到角色卡并保存、刷新与再编辑；2014 既有行为不变；形成可复用检查表。
- 本次范围（自动化）：
  - 12 个 2024 职业在 1／3／4／19／20 级的时间线展开与起始装备方案（选方案 A 后物品非空），对应 AC-01 与 AC-09。
  - 2024 战士（勇士）从创建→起始装备→（无施法跳过）→派生→导出模型→JSON 往返的完整链路。
  - 2024 法师（塑能师）法术书流程：按配置要求选取戏法／法术书／准备后校验通过，派生提供法术攻击加值（AC-07）。
  - 升降级：高等级选择失效后恢复并重新生效（AC-02）。
  - 2014 回归：既有草稿不受 2024 偏好与入口开放影响。
- 不包含：ZIP／PDF／XLSX 承载（B11）；跑团流程（B10）；人工浏览器验收（用户明确要求时才安排）。

## 2. 验证

- 新增 `test/rules/b09-end-to-end-2024.test.ts`（5 项）。
- 全量回归：122 个测试文件、1009 项全部通过；`vue-tsc -b` 通过；`vite-ssg build` 通过；`git diff --check` 无空白错误。
- 无新增用户可见文案，字体子集无需重建。

## 3. 检查表（供其余职业复用）

1. 第一页选版本与等级 → 建卡。
2. 起源（物种／背景／语言／背景属性分配）通过校验。
3. 职业与子职按版本可选，时间线检查点全部完成。
4. 起始装备方案 A／B 二选一，套组展开完整。
5. 施法：有施法配置则完成戏法／法术书／准备；无配置可直接继续。
6. 角色卡派生（HP、AC、攻击、法术）与导出模型无错误诊断。
7. 保存 → 刷新恢复 → 改等级／改子职／改专长 → 校验与派生一致。
8. JSON 往返保留版本、身份与选择。

## 4. 文档同步

TODO 计划（B09-05 勾选）、B09 更新计划（批次状态与 B09 完成说明）、本任务文档。

## 5. 完成标准

- 两条 2024 路径（战士、法师）全绿且有记录；2014 回归全绿。
- 检查表落档，供其余职业复用。
- 门槛全部通过且文档同步完成。

## 6. 结果回填

- 变更：新增端到端验收测试 1 份（5 项）。
- 检查结果：`vue-tsc -b` 通过；`vitest run` 122 文件／1009 项全部通过；`vite-ssg build` 通过；`git diff --check` 无空白错误。
- 剩余问题：跑团资源结算（B10）与导出承载（B11）仍在 B09 声明范围之外；B07-05 完整物品库语义待执行。
- 可勾选 TODO：B09-05（B09 全阶段完成）。
