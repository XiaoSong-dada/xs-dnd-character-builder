# B09-04 角色卡、人工编辑与提示级校验呈现 · 更新任务

- 对应 TODO：B09-04（[B09 更新计划](2014与2024双规则版本支持-B09更新计划.md) 第 4.4 节）。
- 实现日期：2026-09-15。
- 状态：已完成。
- 已接受的产品默认：Q-B09-4 2024 边界提示同时出现在校验页与角色卡（已按此实现）。

## 1. 目标与范围

- 目标：角色卡摘要、能力、攻击、法术与人工编辑入口不再包含 2014 固定文案与假设；提示级问题可区分呈现且不阻断；2024 车卡边界明确。
- 本次范围：
  - `ValidationStep`：错误与提示分组展示（"需要处理"／"提示（不阻断）"），只有提示时给出可继续说明；接受 `ruleset` prop 并在 2024 草稿显示"2024 车卡预览"边界说明。
  - `CharacterSheetStep`：头部版本文案改为草稿版本（原固定 `5e-2014`）；2024 草稿显示车卡预览边界说明（资源结算 B10、导出承载 B11 未完成）。
  - 人工编辑入口与法术／攻击解析已在 B09-02 按版本解析，本批只复核文案与呈现。
  - 诡术师"必须包含法师之手"保持提示级（B08-14 结论），本次由分组呈现确保不阻断完成。
- 不包含：跑团资源结算（B10）与导出承载（B11）；物品库自由添加（B09-06）。

## 2. 文件

| 文件 | 改动 |
| --- | --- |
| `src/views/character-builder/components/ValidationStep.vue` | 错误／提示分组、2024 边界说明、`ruleset` prop |
| `src/views/character-builder/components/CharacterSheetStep.vue` | 版本文案按草稿、2024 车卡预览说明 |
| `src/views/character-builder/index.vue` | 传 `ruleset` 给校验页 |

## 3. 验证

- 新增 `test/components/ValidationStep.test.ts`（4 项）：错误与提示分组、只有提示时可继续、点击跳转步骤、2024 边界说明。
- 扩展 `test/components/CharacterSheetStep.test.ts`：2024 草稿显示版本与车卡预览边界。
- 全量回归：121 个测试文件、1002 项全部通过；`vue-tsc -b` 通过；`vite-ssg build` 通过；`git diff --check` 无空白错误；PDF 字体子集已重建。

## 4. 文档同步

TODO 计划（B09-04 勾选）、B09 更新计划（批次状态）、本任务文档。

## 5. 完成标准

- 2024 角色卡不出现与版本冲突的文案或数值假设；提示级问题可见且不阻断完成。
- 校验页可区分错误与提示，并能跳转到对应步骤处理。
- 门槛全部通过且文档同步完成。

## 6. 结果回填

- 变更：校验页分组呈现与 2024 边界说明、角色卡版本文案与预览说明、校验页接入 `ruleset`；新增／扩展 5 项测试。
- 检查结果：`vue-tsc -b` 通过；`vitest run` 121 文件／1002 项全部通过；`vite-ssg build` 通过；`git diff --check` 无空白错误；PDF 字体子集已重建。
- 剩余问题：物品库自由添加归 B09-06；B09-05 端到端验收随后执行。
- 可勾选 TODO：B09-04。
