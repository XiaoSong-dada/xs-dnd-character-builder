# 背景/出身补全 · G 批次更新计划

- 对应需求：[背景补全-G批次需求](背景补全-G批次需求.md)（决策记录 6.1／6.2 已全部确认，含 Q1—Q11）。
- 数据基准：CHM v2026.09.13（章节号见需求文档第 3 节与附录，**禁止沿用旧版号**）。
- 制定日期：2026-09-20。状态：**实施中**。
- 版本：收尾发 **v1.7.0**（Q7-A），三处同步（`CHANGELOG.md`／`update-notices.ts`／`package.json`）。

## 1. 目标与范围

目标：把 `5e-2014` 官方扩展缺口的 **22 个背景**、第三方缺口的 **33 个背景**（28 条 2014 写法 + 5 条 2024 写法）以及 **2 个索引条目**补齐，并同步资料文档。

| 分类 | 数量 | 落点 |
| --- | ---: | --- |
| 官方扩展背景 | 22 | `app/src/rules/data/origins-2014.ts` + `background-features-2014.ts` |
| 第三方 2014 写法背景 | 28 | 同上（来源默认关闭） |
| 第三方 2024 写法背景 | 5 | `app/src/rules/data/origins-2024.ts`（2024 仓库） |
| 第三方起源专长（Q10-A） | 待定（DoD 4 条固定授予） | 2024 专长数据 |
| 第三方起始装备（Q11-A） | 5 套 A/B | `app/src/rules/data/starting-equipment-2024.ts` |
| 2024 第三方来源（Q8-B） | 2 | `app/src/rules/data/sources-2024.ts` |
| 2014 新来源 | 1（GoS） | `app/src/rules/data/sources-2014.ts` |
| 索引条目 | 2 | EGtW 英雄编年史、GoS 盐沼 DM 工具页 |
| 资料文档 | 约 57 份 | `docs/backgrounds/<规则集>/<id>/<id>.md` + `docs/dnd-backgrounds.md` |

## 2. 实施小批与顺序

按需求文档第 5 节的 6 个小批执行；**先代码（可运行、可验证），后资料文档**（文档量大，按批补齐）。

| 小批 | 内容 | 数量 | 状态 |
| --- | --- | ---: | --- |
| G-A | GoS（含来源注册）+ AI 3 + EGtW 2（含英雄编年史索引页） | 11 | **已完成**（`b397a3a`） |
| G-B | DSotDQ 2 + SatO 2 + AAG 2 + VRGtR 1（GGR 1 因 CHM 缺页跳过） | 7 | **已完成**（`9440af9`） |
| G-C | SCC 5 学生 | 5 | **已完成**（`5cd67f3`） |
| G-D | 第三方 2014 写法：TAL 4 + OBO 6 | 10 | **已完成**（`3f2e996`） |
| G-E | 第三方 2014 写法：DoD 5 | 5 | **已完成**（`3f2e996`） |
| G-F1 | 2024 第三方来源 3 个 + 第三方起源专长 14 条 | 17 | **已完成**（`eaa7b98`） |
| G-F2 | 第三方 2024 写法背景 18 条（CM 13 + DoD 4 + CBT 1）与装备映射 | 18 | **已完成**（`55b48c0`） |
| G-G | 资料文档：`docs/backgrounds/**` 与索引表 | 54 份 | **已完成**（`23fcfe4`） |
| G-H | 发版与验证：v1.7.0、三处同步、全量验证 | — | **已完成**（`6a579dd`） |

## 7. 完成记录

实施日期：2026-09-20。状态：**已完成（G-A—G-H）**。版本：**v1.7.0**。

### 7.1 提交记录（从新到旧）

| 提交 | 内容 |
| --- | --- |
| `6a579dd` | v1.7.0 发版登记（CHANGELOG／update-notices／package.json 三处同步） |
| `23fcfe4` | G-G：54 份背景资料文件 + `dnd-backgrounds.md` 四节索引表 |
| `55b48c0` | G-F2：18 条第三方 2024 背景 + 装备方案 |
| `eaa7b98` | G-F1：3 个 2024 第三方来源 + 14 条第三方起源专长 |
| `3f2e996` | G-D／G-E：第三方 2014 写法 15 个背景 |
| `5cd67f3` | G-C：斯翠海文五学院学生 5 个背景 |
| `9440af9` | G-B：龙枪／印记城／星界／鸦阁 7 个背景 |
| `b397a3a` | G-A：盐沼怪谈／艾奎兹玄／荒洲 9 个背景 |

### 7.2 最终规模

| 规则集 | 背景 | 其中第三方 | 背景特性 | 专长 |
| --- | ---: | ---: | ---: | ---: |
| `5e-2014` | **76**（基础 71 + 变体 5） | 15 | 80 | 72 |
| `5e-2024` | **34**（核心 16 + 第三方 18） | 18 | 0（2024 无背景特性） | 127（含第三方 14） |

### 7.3 验证结果

- `npx vue-tsc -b`：通过。
- `npx vitest run`：**150 个文件 / 1232 项全部通过**（过程中修正 9 处计数断言为覆盖／下界口径）。
- 生产构建（`generate:items` + `vue-tsc -b` + `vite-ssg build`）：通过。
- 运行期复核：0 重复 ID、0 缺特性背景、0 未解析装备引用、跨版本 ID 冲突 0、第三方来源默认全部关闭、模板残留 0。
- PDF 字体子集：因新增文本引入未覆盖字形，重建 3 次（最终 1179.1 KB）。
- 文档链接校验：索引内 54 条新增链接全部可解析（0 缺失）。

### 7.4 与计划的偏差

| 项 | 说明 |
| --- | --- |
| 官方缺口 22 → 21 | GGR 析米克科学家在 CHM v2026.09.13 中检索不到背景页，无法核验，本批跳过 |
| 第三方写法分布修正 | 实施中第二次精确核验后，2024 写法由 5 条修正为 **18 条**（歪曲之月 13 条全部为 2024 写法），相应把 G-D／G-E 缩到 15 条、G-F 扩为 G-F1＋G-F2 两步 |
| 索引条目实现方式 | 盐沼背景 DM 工具页与英雄编年史**只登记资料文档**、不写入运行时仓库——背景候选不按 `status` 过滤，入库会被当作普通可选；如需入库须先补「背景候选按 status 过滤」的实现 |
| 2024 第三方背景属性 | 原书 14 条不含属性提升行，按原书留空 `abilityChoices`，未替原书编造候选 |
| 资料文档生成方式 | 用一次性 Vite SSR 脚本从运行期数据导出，保证文档与代码一致；脚本跑完已删除 |

### 2.2 已完成小批的实施记录

**G-A（`b397a3a`）**：GoS 来源（`gos-2019-index`）+ 9 个背景/特性。2014 背景 40 → 49。

**G-B（`9440af9`）**：DSotDQ 索兰尼亚骑士／高等术法会大法师、SatO 位面哲学家／门镇守卫、AAG 星界浪客／荒宇人、VRGtR 精界迷失者，共 7 个背景与 7 条特性。授予专长的 4 条登记为 `selectable` 并注明「不接入自动专长授予，按原书处理」；SatO 的战役先决与派系技能对照表写入描述。49 → 56。

**G-C（`5cd67f3`）**：SCC 五学院学生 5 个背景/特性，按 Q2-A 登记为 `selectable` 摘要。56 → 61。

**G-D／G-E（`3f2e996`）**：第三方 2014 写法 15 个背景与 15 条特性——TAL 学苑学者／扣环帮成员／白石城步枪团／阿沙里人，OBO AHA学徒／亲灵者／机械师／潜水学徒／速递队学员／魔女学徒，DoD 大陆贵族／寻宝者／幸存者／虔诚传教士／魔法血脉。保守登记口径：自选技能按固定技能登记并在描述注明；OBO 自有技能（工程／回收／潜水甲）与自有货币（金花币）无对应条目 → `skillIds` 留空、以文字登记；白石城步枪团的火器不在 2014 装备库 → 仅文字说明；DoD 的 1d8 秘宝／传闻表写入描述。61 → 76。

**G-F1（`eaa7b98`）**：新增 3 个 2024 第三方来源（歪曲之月／德拉肯海姆／火炬光下的克苏鲁，均 `third-party` + 默认关闭；复用既有「合作内容」分区，无需改界面）+ `third-party-feats-2024.ts` 14 条第三方起源专长（CM 12 + DoD 2，ID 前缀 `feat-2024-tp-`，`selectable`）。2024 专长 113 → 127。

**G-F2（`55b48c0`）**：登记 18 条第三方 2024 写法背景（CM 13 + DoD 4 + CBT 1），2024 背景 16 → **34**；为 18 条补 `backgroundEquipmentA2024` 的 A 项装备（B 项沿用 50 GP）并补齐 `fixedToolIds` 的 `tool-2024-smith` 映射，装备引用全部可在 2024 装备库解析。按原书口径：14 条无属性提升行的背景 `abilityChoices` **留空**（不替原书编造候选）；「任选起源专长」与「原书未核验固定专长」的 5 条 `originFeatId` 留空并在描述注明。

**测试加固（跨小批）**：`origins-2014`／`race-features-2014`／`starting-equipment-2014`／`SourcesStep`／`version-aware-steps-2024`／`source-books`／`repositories`／`origins-2024`／`b12-open-scope` 的计数断言统一改为**集合覆盖或下界**断言，后续小批不再需要改测试。

**G-D／G-E 的口径修正（重要）**：实施中经第二次精确核验，第三方写法判据改为「出现属性值、专长、或 A/B+50GP 任一即 2024 写法」，据此歪曲之月 13 条**全部为 2024 写法**（原判 2014 写法有误）。第三方合计：**2014 写法 15 条（已完成）、2024 写法 18 条（G-F2 待办）**。另 GGR 析米克科学家因 CHM 缺页本批跳过。

### 2.1 G-A 实施记录（2026-09-20）

已完成：GoS 来源注册（`gos-2019-index`，沿用 `source()` 工厂 → 自动进入 `SELECTABLE_SOURCE_IDS`）、7 个背景与 7 条背景特性登记（GoS 4 + AI 3 + EGtW 2 已在 G-A 内完成，共 9 条背景）。

| 背景 | ID | 技能 | 工具 | 语言 | 特性 |
| --- | --- | --- | --- | ---: | --- |
| 海军 | `background-2014-marine` | 运动、求生 | 水上载具 | 0 | 稳步前进 |
| 渔民 | `background-2014-fisher` | 历史、求生 | — | 1 | 捕获水产 |
| 船工 | `background-2014-shipwright` | 历史、察觉 | 木匠工具、水上载具 | 0 | 我来修好它 |
| 走私者 | `background-2014-smuggler` | 运动、欺瞒 | 水上载具 | 0 | 灰色地带 |
| 失败商人 | `background-2014-failed-merchant` | 调查、游说 | 工匠工具 | 1 | 供应链 |
| 竞争对手实习生 | `background-2014-rival-intern` | 历史、调查 | 工匠工具 | 1 | 内部线人 |
| 赌徒 | `background-2014-gambler` | 欺瞒、洞悉 | 赌具 | 1 | 永远别告诉我赔率 |
| 笑面人 | `background-2014-grinner` | 欺瞒、表演 | 乐器、盗贼工具 | 0 | 偏好乐曲 |
| 沃什塔克特工 | `background-2014-volstrucker-agent` | 欺瞒、隐匿 | 制毒工具 | 1 | 悲剧 |

验证：`vue-tsc -b` 通过；运行期复核 2014 背景 40 → **49**、重复 ID 0、无缺特性背景。

**索引条目设计约束（重要，需在 G-A 收尾时确认）**：`BackgroundRule` 虽有 `status` 字段，但 `OriginStep.vue` 的背景候选只按 `parentBackgroundId` 与来源开关过滤（第 54—56 行），**不按 `status` 过滤**。因此把「盐沼背景 DM 工具页」「英雄编年史出身故事」以 `index-only`／`dm-only` 入库会让它们出现在车卡候选里、且可被选中，直接违背 Q5-B／Q6-B 的「不进入普通车卡」目标。当前处理：**这两条只在资料文档（`docs/dnd-backgrounds.md`）登记为索引条目，不写入运行时仓库**；如确需入库，需先补「背景候选按 status 过滤」的实现（属模型改动，另行确认）。

## 3. 关键实现口径（已定）

1. **2014 背景**：`origin()` 式登记，字段为 `skillIds`／`toolIds`／`languageChoices`／`featureName`／`description`／`variantIds`／`status`／`sourceIds`；装备要点写入 `description`（2014 惯例）。
2. **背景特性**：每条基础背景 1 条 `BackgroundFeature`（`implemented` 或 `selectable`，按效果是否可自动计算判定）。
3. **第三方来源**：2014 侧沿用已注册的 `tp-*` 来源（`contentKind: 'third-party'`、`defaultEnabled: false`）；2024 侧新增 2 个 `source-2024-*` 第三方来源。
4. **Q2-A／Q3-A**：SCC 5 学生、SatO 2 条、DSotDQ 2 条登记为 `selectable` 摘要，不接入专长授予与位阶选择。
5. **Q8-B**：5 条 2024 写法背景只进 `5e-2024` 仓库；属性候选按原书三项；神话调查员的「任选」按全部六项登记并在摘要标注自选。
6. **Q10-A**：DoD 4 条背景的固定起源专长一并登记为 2024 第三方专长（`selectable`、来源默认关闭）。
7. **Q9-A**：每个新增背景提供 8 项人格特征／6 理想／6 羁绊／6 缺点（项目原创转述）。
8. **德拉肯海姆 1d8 秘宝／传闻表**：写入 `description`，不建结构化表。
9. **术语**：一律以 5e 不全书用字为准（沿用术语批次 Q10 口径）。
10. **拳斗士**（`4889`）为第三方职业，不作为背景登记。

## 4. 验证计划

1. `npx vue-tsc -b`；
2. `npx vitest run`（含 `background-descriptions-2014.test.ts`、`race-features-2014.test.ts`、`starting-equipment-2014.test.ts`、`unit-consistency.test.ts`；计数类断言按需同步）；
3. 生产构建 `npm run build`；
4. 运行期复核脚本：按来源统计新增背景数与特性数、确认第三方来源默认关闭、确认 2014／2024 无同 ID 冲突；
5. 若新增文本引入未覆盖字形 → 重建 `app/public/templates/fonts/noto-sans-sc-subset.ttf`（`python tools-subset/make-subset.py`）；
6. 依赖拓扑复核并在交付说明中写明结论。

## 5. 风险

| 风险 | 处理 |
| --- | --- |
| 57 项资料文档（8/6/6/6 特征表）工作量最大 | 代码先行、文档按批补齐；文档可独立成批（G-G），不阻塞代码验收 |
| 第三方 2024 背景需要起源专长与装备数据 | Q10-A／Q11-A 已确认一并登记；若某专长在原书仅有名称与摘要，按 `selectable` 登记 |
| 神话调查员「任选属性」超出三项候选类型 | 登记为六项候选 + 摘要标注「原书为任选」，不新增类型字段（如确需新增，先报告） |
| 既有计数断言 | 已列入第 4 节第 2 项 |
| SCC／SatO 的专长与战役先决 | Q2-A 已定：只登记摘要并显式标注 |
| 字体子集 | 已列入第 4 节第 5 项 |

## 6. 完成记录

（实施后填写：实际改动文件、验证结果、提交哈希、偏差说明。）
