# S05 收尾：文档、版本与全量验收 · 更新计划

- 对应 TODO：S05-01—S05-05（[分批 TODO 计划](法术库补全-TODO计划.md)）。
- 依据需求：R8、R9（[法术库补全需求](法术库补全需求.md)）；发布口径 v1.5.0。
- 制定日期：2026-09-18。状态：**已完成（2026-09-18）**。依赖：S01—S04 全部完成。

## 1. 目标与范围

目标：在 S01—S04 全部落地后，完成文档终审、PDF 字体子集更新、v1.5.0 三处发布登记与全量验收，并把需求文档「实施状态」章节补全。

范围内：

| 域 | 内容 |
| --- | --- |
| 文档 | `docs/rules.md`、`docs/quick-build-implementation.md`、`docs/dnd-classes.md`（S02 子职）、需求文档实施状态 |
| 字体 | `app/public/templates/fonts/noto-sans-sc-subset.ttf` 重新生成 |
| 版本 | `CHANGELOG.md`、`app/src/constants/update-notices.ts`、`app/package.json` 三处同步 |
| 验收 | 全量测试、类型检查、生产构建、依赖拓扑复核 |

范围外：新功能；第三方其他内容；自动比对校验（D-06）；README 版本信息（明确不写）。

## 2. 任务分解

- [ ] S05-01 文档终审：
  - `docs/rules.md`：法术数据范围（2014 489 → 775、来源表 6+11、2024 旧扩展 128 条与默认关闭口径）、2024 扩展登记约定（UA 跨职业收口）；
  - `docs/quick-build-implementation.md`：法术条数与来源说明同步；
  - `docs/dnd-classes.md`：S02 的两个 EGtW 法师子职与特性；
  - `docs/需求文档/法术库补全需求.md`：「实施状态」章节逐条标注 R1—R9 与 S00 决策；
  - 如新增模块或改变依赖方向，同步 `docs/frontend-architecture.md`。
  - 验收：文档与代码事实一致，无过期数字（如 489／"VRGR 与 BGG 法术"等旧表述已修正）。
- [ ] S05-02 PDF 字体子集：
  - 汇总新增法术名与摘要用字（含第三方与 EGtW 的中文用字），按既有流程重新生成 `noto-sans-sc-subset.ttf`；
  - 导出回归：PDF 三页模板填充与中文字形正常、XLSX／JSON／ZIP 结构不变。
  - 验收：无缺字／方框，导出体积与既有口径同量级。
- [ ] S05-03 版本登记：
  - `CHANGELOG.md`（v1.5.0 增补法术补全条目）、`app/src/constants/update-notices.ts`（面向用户条目）、`app/package.json`（仍为 1.5.0）三处一致；
  - 更新公告内容要点：官方 20 条、EGtW 15 条、第三方 251 条（合作内容、默认关闭、需 DM 同意）、UA 跨职业、2024 旧扩展层（默认关闭、未在 PHB 2024 重印）；
  - README 不新增／保留任何版本信息。
  - 验收：三处版本条目一致，README 合规。
- [ ] S05-04 全量验收：
  - `vitest run` 全部通过；`vue-tsc -b` 通过；`npm run build` 通过（含预构建无差异）；
  - 依赖拓扑复核：新增均为 `rules/data` 与既有页面组件，无逆向依赖；
  - 不新增浏览器控制台错误（新增 UI 分组与标签）。
  - 验收：全量通过；交付说明写明「已复核前端依赖拓扑，已更新／无需更新」。
- [ ] S05-05 实施状态与遗留：
  - 需求文档「实施状态」标注 R1—R9、S00-01—S00-07 与各批完成度；
  - 遗留登记：EGtW「非秘迹使」授予、第三方其他内容、UA 奇械师对 TCoE 2014 法术兼容、第三方条目核对中未闭环项。
  - 验收：遗留项可追踪，不阻塞发布。

## 3. 影响文件（预计）

| 文件 | 改动 |
| --- | --- |
| `docs/rules.md`、`docs/quick-build-implementation.md`、`docs/dnd-classes.md`、`docs/frontend-architecture.md`（如需要） | 文档同步 |
| `docs/需求文档/法术库补全需求.md` | 实施状态 |
| `app/public/templates/fonts/noto-sans-sc-subset.ttf` | 字体子集更新 |
| `CHANGELOG.md`、`app/src/constants/update-notices.ts`、`app/package.json` | v1.5.0 登记 |

## 4. 验证

1. 全量 `vitest run`、`vue-tsc -b`、`npm run build`。
2. 导出抽样：PDF 中文字形、XLSX 自动计算、JSON／ZIP 往返（含新增与旧扩展条目）。
3. 文档一致性：条数、来源表、默认开关口径与代码一致。
4. 版本三处一致；README 无版本信息。
5. 按项目约定，不要求人工点击或浏览器视觉检查作为完成条件（用户明确要求时例外）。

## 5. 风险与决策点

| 风险 | 控制 |
| --- | --- |
| 新增中文字符导致 PDF 缺字 | S05-02 用既有生成流程汇总字符并回归导出 |
| 文档数字与代码漂移 | S05-01 逐项核对条数与来源表；需求文档实施状态作为最终口径 |
| 版本条目三处不一致 | 按 AGENTS.md 修改流程第 8 条同步 `CHANGELOG.md`＋`update-notices.ts`＋`package.json` |
| 剩余遗留被误当已完成 | S05-05 在需求文档与 TODO 计划中显式登记遗留与影响 |

## 6. 完成记录

实施日期：2026-09-18。

- 文档：`docs/rules.md`（774 条、第三方 11 系列、2024 旧扩展 128 条、UA 跨职业）、`docs/quick-build-implementation.md`（509 → 774 拆分修正）、`docs/dnd-classes.md` 子职链接与两个子职文档已同步。
- 字体：`noto-sans-sc-subset.ttf` 随 S02/S03/S04 重建，覆盖新增法术名与摘要用字。
- 版本：`CHANGELOG.md` 与 `app/src/constants/update-notices.ts` 的 v1.5.0 条目同步补充法术补全说明；`app/package.json` 保持 1.5.0；README 无版本信息。
- 验证：`vitest run` 148 文件 / 1194 项通过；`vue-tsc -b` 通过；生产构建通过；依赖拓扑无需更新。
- 遗留：2024 子职级施法（奥法骑士／诡术师）未纳入 UA／旧扩展扩表；第三方召唤与随机表条目保持提示级；生成脚本需在 2014 旧扩展数据变化后重跑。

