# 车卡体验优化 · 分批 TODO 计划

> 制定日期：2026-09-16；决策确认日期：2026-09-16。
> 依据：[《车卡体验优化需求》](车卡体验优化需求.md)（含已确认决策记录）。
> 状态：**实施中**。U00-01—U00-03 已完成（Q-1—Q-6、Q-4a 已确认）；**U01 已完成（U01-01—U01-04）**；U00-04、U00-05 与 U02、U03、U04 待执行。
> 本计划不修改业务代码；不确定发布日期与函数签名。

## 1. 使用方式与计划边界

- 工作对象为 `app` 产品项目；不修改视觉原型项目。
- 三项需求互相独立，U01、U02、U03 可并行推进；U04 依赖前三批全部完成。
- 每个 TODO 都能独立展开成一个更新任务；后续更新任务必须引用对应编号。
- 进入实现前先完成 U00 的输入准备（U00-01—U00-03 决策已确认，U00-04／U00-05 待执行）；未完成的准备项只阻断依赖它的批次，不阻断无关批次。
- 更新任务保持小范围：一批只改一类数据或一条用户流程，避免「一次性把 589 处单位全改完」的巨型任务。
- 不属于本计划的顺手改动（例如未列出的数值错误修复）登记为后续批次，不混入本批。

### 1.1 编号与勾选约定

- `Uxx` 为批次，`Uxx-nn` 为可追踪 TODO。
- `[ ]` 表示未完成；只有交付物存在且完成标准通过，才改为 `[x]`。
- 「已创建更新任务」不等于「TODO 已完成」。
- 如需细拆，沿用父编号增加后缀（如 `U03-02a`），保留原编号与依赖关系。

## 2. 批次总览

| 批次 | 主题 | 主要前置 | 可交付结果 | 规模 |
| --- | --- | --- | --- | --- |
| U00 | 决策确认与输入准备 | 需求文档 | 决策已确认（见下）；职业介绍取材清单、数值回源取舍待补 | 小 |
| U01 | 法术列表环位排序（R1） | U00（Q-1、Q-2） | 规则层排序出口 + 角色卡/跑团助手接入 + 回归测试 | 小 |
| U02 | 职业详情（R2） | U00（Q-3、Q-4、U00-04） | 25 个职业介绍数据 + 职业卡片「职业详情」区 | 中 |
| U03 | 单位统一为尺/里（R3） | U00（Q-5） | 2014 数据与装备文档消除米制 + 单位防回归测试 | 大（分 7 小批） |
| U04 | 收尾：文档、版本与全量验收 | U01—U03 | 文档同步、更新公告、全量验证通过 | 小 |

## 3. 逐批 TODO

### U00：决策确认与输入准备

目标：把需求文档中的 7 个决策点转为明确结论，并准备 R2 的取材清单，避免实现期反复。

决策已全部确认（2026-09-16），结论与落地位置见需求文档 §6「决策记录」：

- [x] U00-01：Q-1（同环次级排序＝规则表数据顺序）与 Q-2（采纳环级小标题）已确认。
- [x] U00-02：Q-3（字段名 `ClassRule.introduction`）、Q-4（展示生命骰与豁免）、Q-4a（2014 录入 `classPreviews2014` 并修 `repository.ts` 合并顺序）已确认。
- [x] U00-03：Q-5（不保留米制辅助换算）与 Q-6（发版并登记更新公告）已确认。
- [ ] U00-04（待执行，U02 前置）：整理 25 个职业的取材清单：每个职业记录 5e 不全书的章节文件路径、主要属性来源（2024「主要属性」字段／2014「快速建卡」建议）、核对 `ClassRule.primaryAbilities` 是否一致。
- [ ] U00-05（待执行）：确认 R3 的数值回源清单（需求文档 4.2-F 表）的处理方式。由已确认决策推导：随 R3-2 一并纳入本轮；其中「干燥粉尘 45 立方米」来源未核验，先按来源核验结果处理，核验不到时保留为登记项，不猜测数值。

交付物：决策记录（已写入需求文档 §6）、职业取材清单（待补）。

完成标准：U00-01—U00-03 已完成；取材清单可支撑 U02-02／U02-03 直接录入；数值回源项有明确取舍。

### U01：法术列表环位排序（R1）

目标：法术书相关的列表（角色卡三块 + 跑团助手一块）在角色卡与跑团助手中稳定按环级升序显示，且不改变草稿存储顺序。

- [x] U01-01（已完成）：`rules/spellcasting.ts` 新增 `groupSpellsByLevel(spells, ruleset)`：环级升序 + 同环规则表登记顺序（按规则集缓存下标）；单测 5 项（空输入、乱序输入、同环倒序输入、扩展书法术保持登记顺序、不修改传入数组）。
- [x] U01-02（已完成）：`CharacterSheetStep.vue` 三个列表改为消费 `wizardPrepareFromBookGroups` / `spellbookGroups` / `wizardWriteToBookGroups`，并补环级小标题（复用 `.character-sheet__spell-level` + `h5`）。
- [x] U01-03（已完成）：`useSessionPanel.ts` 新增并导出 `unpreparedFromBookGroups`（人工添加法术并入后统一排序）；`SessionPanel.vue` 按分组渲染 + `h4` 小标题，新增 `&__level` 样式。
- [x] U01-04（已完成）：新增角色卡组件断言（三块列表顺序 + 小标题）与跑团助手断言（含人工添加法术）；草稿 `spellSelections` 存储顺序未改写（仅展示派生层排序）；`docs/quick-build-implementation.md` 已同步。

交付物：`rules/spellcasting.ts` 排序函数 + 单测；两处页面接入；组件测试；文档更新。

实施记录（2026-09-17，见 [U01 更新计划](车卡体验优化-U01更新计划.md)）：已全部完成；`vitest run` 134 文件 / 1124 项通过，`vue-tsc -b` 与 `vite-ssg build` 通过。遗留：角色卡「已准备/已掌握」与「全部职业法术」按 R1 非目标未改（同环内仍为选择顺序），如需统一另开批次。

完成标准：

- 规则层单测与组件测试通过；乱序草稿显示为环级升序。
- 草稿存储顺序不变，导入导出往返一致。
- `docs/frontend-architecture.md` 无拓扑变化（未新增模块），交付说明写明「已复核，无需更新」。

规模估计：0.5—1 天。风险：低。注意按 Q-1 已确认结论固定为「规则表数据顺序」次级排序，不引入 `localeCompare` 中文名排序，避免不同环境下顺序不一致。

### U02：职业详情（R2）

目标：第 3 步职业卡片的展开区在「职业成长」上方展示「职业详情」（职业介绍 + 主要属性）。

- [ ] U02-01（依赖 U00-02、U00-04）：`app/src/types/rules.ts` 的 `ClassRule` 新增 `introduction` 字段（Q-3 已确认，设为必填），补注释说明「原创中文转述」；确认 `ClassPreview = Omit<ClassRule, 'checkpoints'>` 会因此强制 13 条预览补齐。
- [ ] U02-02（依赖 U02-01）：在 `rules/data/classes-2014.ts` 的 `classPreviews2014` 集中录入 2014 的 13 条职业介绍；**同步修正 `rules/repository.ts:37-41` 的合并顺序为 `{ ...item, ...classRule, features }`**（否则完整规则会覆盖预览字段，介绍丢失）；逐条核对 `primaryAbilities` 与「快速建卡」建议一致。
- [ ] U02-03（依赖 U02-01）：在各 `<职业>-2024.ts` 录入 2024 的 12 条职业介绍（`classes-2024.ts` 仅装配，无需改动）；逐条核对「主要属性 Primary Ability」字段。
- [ ] U02-04（依赖 U02-01）：`ClassStep.vue` 展开区新增「职业详情」区（位于「职业成长」之前），展示 `introduction` + 主要属性（`primaryAbilities` + `ABILITY_LABELS`）+ 生命骰 + 豁免熟练（Q-4 已确认全部展示）；样式沿用 `ClassStep` 私有 scoped SCSS，遵守嵌套约定与 44px 触控要求。
- [ ] U02-05：测试与文档：新增数据测试（25 个职业介绍非空、非占位、两版隔离、`primaryAbilities` 符合预期）；`test/components/ClassStep.test.ts` 增加展开顺序与内容断言；同步 `docs/dnd-classes.md` §3.1 与对应职业资料文件；同步 `docs/quick-build-implementation.md`。

交付物：`ClassRule` 新字段 + 25 条职业介绍；`ClassStep.vue` 职业详情区；数据与组件测试；文档更新。

完成标准：

- 展开任一职业卡片，可看到职业详情（介绍 + 主要属性）位于职业成长之前；两版职业文本不互相复制。
- 2014 的职业介绍能经 `rulesRepository2014.getClass()` 取到（验证 `repository.ts` 合并顺序已修正，而非只在预览数组中存在）。
- 数据测试覆盖 2014 13 + 2024 12 全部职业。
- 版权口径：全部为原创转述，无原书正文复制。
- 类型检查、测试、构建通过。

规模估计：1—2 天（主要成本在 25 条文本撰写与核对）。风险：中；主要风险是 2014「快速建卡」建议与既有 `primaryAbilities` 不一致，需逐条登记差异并按源书结论处理。

### U03：单位统一为尺/里（R3）

目标：车卡所有文本单位与 5e 不全书一致（尺 / 里 / 磅 / 立方尺 / 加仑），并建立防回归机制。

- [ ] U03-01（前置）：在 `docs/rules.md` 新增「单位约定」小节（尺、里、磅、加仑、立方尺；`5 尺 = 1.5 米` 仅作换算基准）；新增单位审计测试：扫描 `app/src/rules/data/**`，命中 `\d+\s*(米|千克|厘米|公里|英里)` 即失败（`米诺陶`、`析米克`、`麦克斯米利安` 等非单位命中不受影响，因正则要求前置数字）。
- [ ] U03-02（依赖 U03-01）：`spells-2014.ts` 全文改写（397 处米 + 5 处公里 + 4 处千克 + 1 处厘米），按 4.2-F 表回到源书值（千里眼/流星爆 1 里、投影术 500 里、定位动植物与操控天气 5 里、修复术 1 尺、法师之手 10 磅、弹射术 1—5 磅、浮碟 500 磅、念力 1000 磅）。
- [ ] U03-03（依赖 U03-01）：`origins-2014.ts`（71 处）、`subclass-choice-options-2014.ts`（43 处 + 1 处公里）、`fighter.ts`（3 处）改写；同时统一同文件内既有的 17 处 `尺` 写法（空格与用字一致）。
- [ ] U03-04（依赖 U03-01）：`equipment-2014.ts`（38 处 + 1 处千克 + **射程解析正则**）与 `magic-items-2014.ts`（13 处 + 2 处千克）、`magic-items-xgte-tcoe-2014.ts`（8 处）改写；射程改为直接解析 `尺`，移除 `/0.3` 换算；核对 `EquipmentRule.range` 与 2024 数据（`20/60` 尺）口径一致；核对次元袋（64 立方英尺、500 磅）、飞天扫帚（400 磅）等回源值。
- [ ] U03-05（依赖 U03-04）：改写 `docs/equipment/5e-2014/weapons.md`（22 处）、`adventuring-gear.md`（19 处）、`magic-items/*.md`（16 处）；条目名中的单位一并统一（如「麻绳（15 米）」→「麻绳（50 尺）」）；执行 `npm run generate:items` 再生 `generated/magic-items-catalog-2014.ts`，确认生成物无米制残留且无手工编辑。
- [ ] U03-06（依赖 U03-01）：用字统一 `英里` → `里`：`spells-2024.ts`（1）、`background-features-2014.ts`（2）、`subclass-features-2014.ts`（2）、`origins-2014.ts`（1）。
- [ ] U03-07：更新既有测试断言（`test/rules/race-descriptions-2014.test.ts:50-52` 的「10.5 米」「7.5 米」→ 尺），确认单位审计测试通过；同步 `docs/rules.md` 的「法术数据范围」「装备数据范围」口径描述与 `docs/quick-build-implementation.md`。

交付物：单位约定文档 + 审计测试；2014 数据与装备文档全量改写；生成物再生；测试断言更新。

完成标准：

- 单位审计测试通过；`app/src/rules/data/**` 与 `docs/equipment/5e-2014/**` 无 `\d+\s*(米|千克|厘米|公里|英里)`。
- `generated/magic-items-catalog-2014.ts` 与 Markdown 源一致（重新生成后无差异）。
- 4.2-F 表列出的数值已回到源书值。
- 全量测试、类型检查、构建通过。

规模估计：2—4 天（可分包推进，建议按 U03-02 → U03-03 → U03-04 → U03-05 → U03-06 顺序）。风险：中；主要风险是批量替换误伤（如把「米诺陶」「析米克」「麦克斯米利安」改坏），必须限定替换模式为「数字 + 空格 + 米」并逐文件复核。

### U04：收尾

目标：文档、版本与验收齐备。

- [ ] U04-01（Q-6 已确认发版）：`CHANGELOG.md` 与 `app/src/constants/update-notices.ts` 同步登记、`app/package.json` 版本号同步；README 不新增版本信息。
- [ ] U04-02：文档终审：`docs/rules.md`（单位约定）、`docs/dnd-classes.md`（职业字段）、`docs/quick-build-implementation.md`（实施状态）、`docs/frontend-architecture.md`（如新增模块则更新 §8.7；否则交付说明写明「无需更新」）。
- [ ] U04-03：全量验证：`pnpm test`、`vue-tsc -b`、`npm run build`；确认无浏览器控制台错误。
- [ ] U04-04：在需求文档追加「实施状态」小节，逐条标注 R1／R2／R3 的完成情况与遗留项。

交付物：更新公告与版本登记、文档终审记录、全量验证结果。

完成标准：三项需求都有明确的完成或延期标注；自动化验证全部通过；交付说明包含依赖拓扑复核结论。

## 4. 依赖关系图

```text
U00 ──┬─> U01 (U01-01 -> U01-02/U01-03 -> U01-04)
      ├─> U02 (U02-01 -> U02-02/U02-03 -> U02-04 -> U02-05)
      └─> U03 (U03-01 -> U03-02/U03-03/U03-04 -> U03-05 -> U03-06 -> U03-07)
U01 + U02 + U03 ──> U04
```

## 5. 验收清单（跨批次）

| 编号 | 验收项 | 对应需求 |
| --- | --- | --- |
| AC-1 | 规则层排序纯函数单测通过（含乱序、人工添加、空输入） | R1-1、R1-2 |
| AC-2 | 角色卡三个法术书列表与跑团助手「未准备法术」渲染顺序按环升序，并带环级小标题 | R1-1、R1-4 |
| AC-3 | 草稿 `spellSelections` 存储顺序未被改写，JSON 往返一致 | R1-3 |
| AC-4 | 25 个职业介绍非空、两版独立、`primaryAbilities` 与源书一致 | R2-1、R2-6 |
| AC-4a | `rulesRepository2014.getClass()` 能返回 2014 职业介绍（合并顺序已修正） | R2-1a |
| AC-5 | `ClassStep` 展开可见「职业详情」（介绍 + 主要属性 + 生命骰 + 豁免）且位于「职业成长」之前 | R2-2、R2-3 |
| AC-6 | `docs/dnd-classes.md` 与职业资料已同步 | R2-5 |
| AC-7 | `app/src/rules/data/**` 无 `\d+\s*(米|千克|厘米|公里|英里)`；界面与导出无米制换算文案 | R3-1、R3-2、R3-6、R3-7 |
| AC-8 | `docs/equipment/5e-2014/**` 与生成物一致且无米制残留 | R3-4 |
| AC-9 | 射程解析正则改为尺，`EquipmentRule.range` 与 2024 口径一致 | R3-3 |
| AC-10 | 既有测试断言已更新，全量测试、类型检查、构建通过 | 通用 |

## 6. 关联文档

- [《车卡体验优化需求》](车卡体验优化需求.md) — 本计划的输入与验收依据。
- `docs/rules.md` — 单位约定（U03-01）与数据范围口径。
- `docs/dnd-classes.md` — 职业字段约定（U02-05）。
- `docs/frontend-architecture.md` §8.4／§8.7 — 页面与 rules 层拓扑。
- `docs/quick-build-implementation.md` — 车卡实施状态记录。
- `docs/equipment/5e-2014/**` — 装备与魔法物品条目来源（U03-05）。
- 《DND 五版不全书 v2025.11.8》 — 单位与职业资料来源。
