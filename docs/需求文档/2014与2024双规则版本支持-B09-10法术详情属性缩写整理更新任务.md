# B09-10 法术详情属性缩写整理 · 更新任务

- 对应问题：2024 法术**展开后的详情**文本含英文属性缩写（如「DEX豁免」），已整理为中文（「敏捷豁免」）。
- 实现日期：2026-09-15。
- 状态：已完成并提交。
- 依据：[B09-10 更新计划](2014与2024双规则版本支持-B09-10法术详情属性缩写整理更新计划.md)（含只读统计与边界）。

## 1. 范围（严格限定）

- 只改 `src/rules/data/spells-2024.ts` 的 `description` 字段（卡片展开后的详情）。
- 不改：标题行（`formatSpellLabel` 的等级与中英文名）、`englishName`／`name`／`slug` 等字段、DC／AC／CR／GP／DM 惯例缩写、骰式与数值、其它数据文件与源码注释。

## 2. 实施与结果

| 项 | 结果 |
| --- | --- |
| 替换规则 | STR/DEX/CON/INT/WIS/CHA → 力量/敏捷/体质/智力/感知/魅力；HP/HD → 生命值/生命骰；半角 `(技能)` → 全角 `（技能）`；句式、骰式、数值与分隔符不动 |
| 覆盖写法 | `X豁免`、`X(技能)`、`X检定`、`X优势/劣势`、并列列表（如 `STR/DEX/CON`）、其余位置 |
| 替换前 | 属性缩写 208 处 + HP 78 处 + HD 1 处 = **287 处** |
| 替换后 | 六项缩写与 HP／HD 均为 **0 处** |
| 改动条目 | 203 条 `description`（203 行，438 行文件行数不变） |
| 完整性校验 | 用同一替换规则对旧文件重算，逐行与结果完全一致（438/438 行，0 处偏差）→ 除目标词外无任何改动 |
| 抽查 | 燃烧之手「敏捷豁免失败3d6火焰」、次级幻象「智力（调查）」、气化形体「力量/敏捷/体质」、魔魂壶「生命值/生命骰/力量/敏捷/体质」、脆弱诅咒「0生命值」等 |

## 3. 防回归测试

- `test/rules/spells-2024.test.ts`：新增「展开详情使用中文属性与生命值（B09-10）」——断言全部 391 条详情不含 `\b(STR|DEX|CON|INT|WIS|CHA|HP|HD)\b`，并抽查豁免／技能括号／并列列表三类写法。
- `test/rules/spell-descriptions-2014.test.ts`：新增同口径断言（2014 现况通过，作为双版本守护）。

## 4. 验证

- 全量：**125 个测试文件、1035 项全部通过**（新增 2 项）。
- `vue-tsc -b` 通过；`vite-ssg build` 通过；`git diff --check` 无空白错误；PDF 字体子集已重建，字体测试通过。

## 5. 文档同步

B09 更新计划（B09-10 行状态）、TODO 计划（B09-10 项）、`docs/rules.md`（2024 法术详情文案口径）、本任务文档。

## 6. 结果回填

- 变更：`spells-2024.ts` 详情文案 203 条（287 处缩写）中文化；2 处测试断言。
- 剩余问题：无（物种／起源等其它文件按边界不在本批范围）。
- 可勾选 TODO：B09-10（已勾选）。
