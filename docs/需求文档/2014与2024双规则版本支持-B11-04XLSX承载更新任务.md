# B11-04 XLSX 自动计算卡承载适配 · 更新任务

- 对应 TODO：B11-04（[TODO 计划](2014与2024双规则版本支持-TODO计划.md)）。
- 实现日期：2026-09-16。
- 状态：已完成（沿用既有模板与字段映射，不新增列）。

## 1. 范围与设计（`app/src/services/export-xlsx.ts`）

- `splitFeatureText` 在特性条目之后追加 `formatExportResources(model.resources)` 的"资源："行，继续使用 220／700 字符的两级容量与 `content-truncated` 诊断。
- 2024 选择类特性、物种特性、起源专长进入 `features_traits`／`additional_features`；数值与公式列不受影响（`fillTemplate` 仍写输入值并保留公式缓存与 `fullCalcOnLoad`）。

## 2. 验证

- `test/services/export-xlsx.test.ts` 新增 1 项：2024 游侠样例导出，`features_traits`／`additional_features` 含武器精通、猎人猎物与"资源："（宿敌），无 error 诊断。
- 既有模板契约、公式缓存、六表序列化往返测试保持通过。

## 3. 边界

- 不修改 XLSX 模板文件与字段映射表；资源与选择类内容并入既有特性文本字段。
