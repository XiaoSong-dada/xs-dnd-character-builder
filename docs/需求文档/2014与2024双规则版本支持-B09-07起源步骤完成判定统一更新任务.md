# B09-07 起源步骤完成判定统一与语言门禁修复 · 更新任务

- 对应问题：[B09-07 更新计划](2014与2024双规则版本支持-B09-07起源步骤完成判定统一更新计划.md)（用户实际使用 2024 法师＋精灵＋学者时被起源步骤门禁卡住）。
- 实现日期：2026-09-15。
- 状态：已完成（**未提交**，按用户要求等待确认）。
- 已确认决定：Q-B09-7-1 进度同批对齐；Q-B09-7-2 起源页底部清单；Q-B09-7-3 物种施法属性不入闸；Q-B09-7-4 顺带修正固定 2014 文案。

## 1. 根因与修复结果

- 根因：起源步骤门禁使用 2014 语义 `draft.languages.length === background.languageChoices`；2024 背景该字段恒为 0，而必选语言数为 2（＋职业追加）→ 选满语言反而被阻断，0 语言被错误放行。
- 修复：起源完成判定收敛为规则层单一来源 `getOriginStepBlockers` / `isOriginStepComplete`，步骤门禁、起源页提示、完成度与校验共用。

## 2. 共享能力与文件

| 文件 | 改动 |
| --- | --- |
| `src/rules/data/skill-ids.ts` | 新增叶子模块（18 项技能 ID）；`derive.ts` 转为再导出，避免 origins→derive 依赖环 |
| `src/rules/origins.ts` | 新增 `OriginStepBlocker`、`getSpeciesProficiencyBlockers`、`getOriginStepBlockers`、`isOriginStepComplete`（物种链取熟练规格；语言项仅在必选数＞0 时入闸；错误级入闸、提示级不入闸） |
| `src/rules/validate.ts` | 起源类问题改为消费统一判定（id／文案保持兼容）；`origin-required` 文案按版本；物种熟练错误改为映射阻塞项 |
| `src/views/character-builder/hooks/useCharacterBuilderPage.ts` | 起源门禁改用 `isOriginStepComplete`；导出 `originBlockers` |
| `src/views/character-builder/index.vue` | 传入 `:blockers` |
| `src/views/character-builder/components/OriginStep.vue` | 新增 `blockers` prop 与"还差 N 项才能继续"清单（message＋resolution） |
| `src/stores/character-drafts.ts` | 完成度起源项改用 `isOriginStepComplete` |

## 3. 判定项（错误级入闸）

物种缺失、血统缺失／不匹配、背景缺失、语言（数量与重复，仅必选数＞0）、背景属性分配、物种体型、物种技能熟练（数量／非法选项／吉斯洋基互斥）。工具未选与物种施法属性（时间线检查点）保持提示级，不入闸。

## 4. 验证

- 新增 `test/rules/origin-step-completion.test.ts`（9 项）：完整起源通过；缺陷回归（语言 0／1 阻断、2 通过、重复阻断）；血统／物种技能／背景分配／体型；2024 游荡者 3 门；**阻塞项 ⊆ 校验错误**一致性；2014 侍僧 2 门与罪犯 0 门（含多选不阻断）回归。
- 扩展 `test/hooks/useCharacterBuilderPage.test.ts`：2024 起源 `canContinue` 三态（0／1／2 门）。
- 扩展 `test/components/OriginStep.test.ts`：阻塞清单渲染与为空不渲染。
- 全量回归：124 个测试文件、1020 项全部通过；`vue-tsc -b` 通过；`vite-ssg build` 通过；`git diff --check` 无空白错误；PDF 字体子集已重建。

## 5. 文档同步

B09 更新计划（B09-07 批次行与状态）、TODO 计划（B09-07 项）、`docs/frontend-architecture.md`（起源完成判定单一来源）、`docs/rules.md`（起源／语言门禁口径）、B09-07 更新计划（待确认项改为已确认）、本任务文档。

## 6. 结果回填

- 变更：新增 1 个叶子模块与 4 个规则层导出；起源门禁／校验／进度／提示四处统一；新增 11 项测试与 3 处文档说明。
- 检查结果：`vue-tsc -b` 通过；`vitest run` 124 文件／1020 项全部通过；`vite-ssg build` 通过；`git diff --check` 无空白错误；PDF 字体子集已重建（新增"还差 N 项才能继续"等文案）。
- 剩余问题：物种施法属性选择仍在时间线步骤（按计划不入闸）；`language-invalid`（标准表外值）只在校验层报错，不作为步骤阻塞（界面无法产生该值）。
- 可勾选 TODO：B09-07（已勾选）。
