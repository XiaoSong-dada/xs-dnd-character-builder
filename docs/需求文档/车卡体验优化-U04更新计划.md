# U04 收尾（文档终审、发版登记与全量验收）· 更新计划

- 对应 TODO：U04-01—U04-04（[车卡体验优化 · TODO 计划](车卡体验优化-TODO计划.md)）。
- 依据需求：R1／R2／R3（[车卡体验优化需求](车卡体验优化需求.md)）；已确认决策 Q-6（发版并登记更新公告）。
- 制定日期：2026-09-17。
- 状态：**已完成**（2026-09-17）。

## 1. 范围

| 项目 | 内容 |
| --- | --- |
| 版本 | `1.3.0` → **`1.4.0`**（三项用户可见变更：法术列表排序与小标题、职业详情、单位统一） |
| 版本登记 | 根 `CHANGELOG.md`（主公告，全历史）与 `app/src/constants/update-notices.ts`（应用内弹窗）同步，`app/package.json` 版本号同步；README 不新增版本信息（现状已无版本条目） |
| 文档终审 | `docs/rules.md`（单位约定）、`docs/dnd-classes.md`（职业字段）、`docs/quick-build-implementation.md`（实施状态）；`docs/frontend-architecture.md` 如无拓扑变化则交付说明写明「无需更新」 |
| 验收 | `vitest run` 全量、`vue-tsc -b`、`vite-ssg build`；需求文档追加「实施状态」小节逐条标注 R1／R2／R3 |

## 2. 版本登记要点（用户可读）

1. 法术书相关列表（法术书／未准备／未写入法术书）与跑团助手「未准备法术」按环级升序排列并新增环级小标题。
2. 第 3 步职业卡片新增「职业详情」：职业介绍、主要属性、生命骰与豁免熟练。
3. 全站单位统一为《5e 不全书》口径（尺／里／磅／立方尺），法术、种族、职业特性与装备描述中的米制文案全部替换。

## 3. 验证

- 版本一致性由既有测试间接守护：`test/stores/update-notice.test.ts` 要求当前版本存在公告条目，`test/config/site.test.ts` 断言站点版本取自 `package.json`——版本号与公告必须同时更新。
- 全量测试、类型检查、生产构建通过；`generate:items` 再生无差异。

## 4. 完成记录

实施日期：2026-09-17。

### 4.1 改动清单

| 文件 | 改动 |
| --- | --- |
| `app/package.json` | 版本 `1.3.0` → `1.4.0` |
| `CHANGELOG.md` | 新增 `## v1.4.0` 主公告（4 条用户可见变更 + 边界说明） |
| `app/src/constants/update-notices.ts` | 新增 1.4.0 应用内弹窗公告（4 条） |
| `docs/需求文档/车卡体验优化需求.md` | 新增「实施状态（2026-09-17 登记）」；原「关联文档」顺延为 §9 |
| `docs/需求文档/车卡体验优化-TODO计划.md` | U00-05、U04-01—U04-04 勾选与实施记录；状态改为已完成 |
| `docs/需求文档/车卡体验优化-U04更新计划.md` | 本文件 |

### 4.2 验证结果

- `vitest run`：**136 个测试文件 / 1134 项全部通过**（含 `test/stores/update-notice.test.ts` 要求当前版本存在公告条目、`test/config/site.test.ts` 断言站点版本取自 `package.json`——版本号与公告三处同步均被测试覆盖）。
- `vue-tsc -b`、`vite-ssg build` 通过（6 个预渲染页面）。
- `npm run prebuild`（`generate:items`）无差异。
- 依赖拓扑：未新增模块，`docs/frontend-architecture.md` 无需更新。

### 4.3 本阶段交付总结

| 阶段 | 提交 |
| --- | --- |
| U01 法术列表环位排序 | `750663f` |
| U02 职业详情 | `df1f621` |
| U03 单位统一 | `fd2dd50` |
| U04 收尾（v1.4.0） | 本次提交 |

遗留项（不阻塞）：已准备／已掌握同环排序（R1 非目标）；3 处规则数值差异与 B01 核验记录用字；干燥粉尘按选项 (b) 处理。
