# 法术仪式标记补全 · 更新计划

> 制定日期：2026-09-18。依据需求：[《法术仪式标记补全需求》](法术仪式标记补全需求.md)（R1—R8）。
> **版本口径：仍为 v1.5.0，不升级版本号**（用户 2026-09-18 明确确认）。
> 状态：**已完成（2026-09-18）**。D1 按建议取 A（补标），全部验证通过。
> 本计划只确定任务、依赖与验收，不承诺函数签名；实施时逐项引用任务编号。

## 1. 目标与范围

**目标**：为不全书中具有仪式依据、但项目未标记的 2014 第三方合作法术补 `ritual: true`，补齐回归测试，并完成文档同步与 v1.5.0 内登记；不新增法术、不改效果数据、不升级版本号。

范围内：

| 域 | 内容 |
| --- | --- |
| 数据 | `app/src/rules/data/spells-2014.ts` 的 `ritualSpellNames2014` 增补 8 条（深根入月按决策门结论） |
| 测试 | 仪式标记回归断言；抽查标签输出 |
| 文档 | `docs/rules.md` 第三方段落补注、需求文档实施状态、依赖拓扑复核 |
| 版本 | `CHANGELOG.md` 与 `app/src/constants/update-notices.ts` 的 v1.5.0 条目追加；`app/package.json` 保持 `1.5.0` |

范围外：`Lucky Charm` 与 `Summon Lesser Demon` 的译名／来源核对（另行登记）；2024 数据；与 CHM 的自动比对校验（沿用 D-06）；其他法术字段与效果数值。

## 2. 决策门

| 编号 | 决策项 | 选项 | 建议与理由 |
| --- | --- | --- | --- |
| D1 | `Deep Roots of the Moon`（深根入月）是否补仪式标记 | A 按不全书补；B 跳过并登记遗留 | **A**：不全书是项目登记基线；其摘要已按「1 动作」呈现，补 `ritual` 不改变现有数值展示，风险仅为来源书可能的排版错误，已在需求文档备注。**实施取 A。** |

> 已知影响（非决策项，需接受）：保持 v1.5.0 时，`app/src/stores/update-notice.ts` 以 `lastSeenVersion !== currentVersion` 判断弹窗，已关闭 1.5.0 公告的用户**不会**因本次追加条目再次自动看到弹窗，只能通过「更新公告」手动入口查看。若要求弹窗重新触发，必须升级版本号（与本次口径冲突）。

## 3. 任务分解

### T01 数据补标

- [x] T01-01 在 `ritualSpellNames2014` 末尾（第三方补录分组，`Shadow Gateway` 之后）按序追加：
  `Sand Structure`、`Cursed Cacophony`、`Contact Deity`、`Clue`、`Memorize`、`Game of Fate`、`Faerie Toast`、`Charnel Banquet`；**D1=A 时**追加 `Deep Roots of the Moon`。
- [x] T01-02 复跑 `tmp/spell-audit/ritual_compare_full.py`：缺口 0、反向冲突 0；CHM 全部仪式法术在项目中均有仪式标记。
- 验收：上述 8（或 9）条 `ritual === true`；其余法术标记不变；不新增法术条目。

### T02 回归测试

- [x] T02-01 在 `app/test/rules/spell-label.test.ts` 增补断言：清单内每条 `englishName` 对应法术 `ritual` 为 `true`；保留既有 `ritualSpellNames2014` 集合一致性断言（集合与 `spells2014` 过滤结果一致）。
- [x] T02-02 抽查 `src/utils/format-spell-label.ts` 输出含「· 仪式」；如既有测试已覆盖则引用用例名，不重复造测试。
- [x] T02-03 如 `third-party-spells-2014.test.ts` 存在更贴近的第三方断言位置，可择一放断言，避免同义重复。
- 验收：`npm run test:run` 通过；删除任一新标记会使测试失败（可验证断言有效性）。

### T03 文档同步

- [x] T03-01 `docs/rules.md` 第三方合作内容段落（第 244 行「S03」条目）补注：9 条按不全书补仪式标记；法术总数 774 不变。
- [x] T03-02 需求文档《法术仪式标记补全需求》「实施状态」章节标注 R1—R8 与 D1 结论。
- [x] T03-03 复核 `docs/frontend-architecture.md` 当前拓扑：改动仅 `src/rules/data` 与测试，预计无需更新，交付说明写明结论。
- 验收：文档与代码事实一致，无过期数字。

### T04 v1.5.0 内登记（不升版本）

- [x] T04-01 `CHANGELOG.md` v1.5.0 条目末尾追加一条（要点：第三方合作法术 9 条按《5e 不全书》补仪式标记；法术数量与来源范围不变）。
- [x] T04-02 `app/src/constants/update-notices.ts` v1.5.0 `items` 追加同要点条目（内容与 CHANGELOG 一致，面向用户表述）。
- [x] T04-03 `app/package.json` `version` 保持 `1.5.0` 不变；各 README 不新增版本信息。
- 验收：两处公告要点一致、package.json 未变；更新公告手动入口可读到新条目。

### T05 验证与交付

- [x] T05-01 执行：`cd app && npm run test:run`、`npx vue-tsc -b`、`npm run build`。
- [x] T05-02 复跑核对脚本并保留输出；确认缺口/反向冲突均为 0。
- [x] T05-03 交付说明：写明改动范围、验证结果、依赖拓扑复核结论；不执行 git 提交（除非用户明确要求）。
- 验收：类型检查、测试、生产构建全部通过；无浏览器控制台相关新增风险（纯数据改动）。

## 4. 影响文件（预计）

| 文件 | 改动 |
| --- | --- |
| `app/src/rules/data/spells-2014.ts` | `ritualSpellNames2014` 增补 8—9 条英文名 |
| `app/test/rules/spell-label.test.ts`（或 `third-party-spells-2014.test.ts`） | 回归断言 |
| `docs/rules.md` | 第三方段落补注（数量不变） |
| `docs/需求文档/法术仪式标记补全需求.md` | 实施状态 |
| `CHANGELOG.md`、`app/src/constants/update-notices.ts` | v1.5.0 条目追加 |
| `app/package.json` | 不改（保持 1.5.0） |

## 5. 验证命令

1. 批量补标核对：`python3 tmp/spell-audit/ritual_compare_full.py`
2. 类型检查与测试：`cd app && npx vue-tsc -b && npm run test:run`
3. 生产构建：`cd app && npm run build`
4. 反向抽查：随机抽取 2—3 条新标记法术，确认候选池与「· 仪式」标签显示正常（纯数据路径，可不做浏览器验收）。

## 6. 风险与回退

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 深根入月来源书与不全书标记不一致 | 单条标记可信度 | D1 决策；如选 A 则登记来源差异备注 |
| 保持 v1.5.0 导致公告弹窗不重触发 | 老用户看不到新增条目 | 已知影响；靠手动入口与更新日志承载 |
| 第三方来源默认关闭 | 实际影响面小 | 属预期：需 DM 开启「合作内容」 |
| 误改其他法术字段 | 数据回归 | 仅改 `ritualSpellNames2014` 一处；测试守护 |

**回退**：移除新增英文名与测试断言即可，无数据迁移、无草稿兼容影响。

## 7. 实施状态

**已全部完成（2026-09-18）**，D1=A。

| 任务 | 结果 |
| --- | --- |
| T01 数据补标 | `ritualSpellNames2014` 已增补 9 条；复跑 `ritual_compare_full.py`：缺口 0、反向冲突 0 |
| T02 回归测试 | `test/rules/spell-label.test.ts` 新增第三方仪式清单断言与「1环 · Sand Structure · 仪式」标签断言；目标文件 4 项测试通过 |
| T03 文档同步 | `docs/rules.md` S03 段落补注修订；本计划与需求文档实施状态已回填；依赖拓扑无变化 |
| T04 v1.5.0 登记 | `CHANGELOG.md` 与 `app/src/constants/update-notices.ts` 已追加同要点条目；`app/package.json` 保持 `1.5.0` |
| T05 验证 | 全量 `vitest run`：148 个文件、1195 项测试全部通过；`vue-tsc -b` 与 `npm run build` 均通过（退出码 0） |

遗留：`Lucky Charm` 与 `Summon Lesser Demons` 名称／来源核对；本次未执行 git 提交。
