# 快速车卡实施状态

## 当前规则基线

正式应用当前只支持 `5e-2014`，目标为 1—20 级单职业移动端车卡。界面不显示规则版本开关，也不把 2024 内容作为兼容选项。旧 `5e-2024` 草稿从 v1 存储中只读隔离，可原样导出，不会自动转换或删除。

## 已完成纵向切片

- 13 个 2014 职业（含工匠）按稳定顺序浏览；第 2 步改为扩展书选择，职业玩法偏好、评分与推荐徽章已移除。新草稿默认启用当前全部可选 2014 来源，核心永久启用；种族/背景的职业提示仍从规则数据推导。
- 9 个核心种族、对应子种族、13 个背景和 5 个正式背景变体可自由组合。
- 变体人类标记为“2014 可选规则”，并生成 1 级专长检查点。
- 属性来源按“基础值 → 种族 → 子种族 → 最终值”计算。
- 标准数组、27 点购点和自定义属性分别校验。
- 10 级战斗大师战士时间线覆盖职业技能、战斗风格、3 级子职与战技、4/6/8 级属性提升或专长、7/10 级新增战技；不存在武器精通。
- 野蛮人、武僧、游荡者已接入1—20级2014选择时间线、职业装备、豁免、无甲防御/移动或专精派生。
- 圣武士与游侠已接入1—20级选择时间线、职业装备、2级半施法、准备/已知法术校验，以及法术攻击和法术豁免 DC 来源。
- 法师已接入戏法、法术书、准备法术三层校验；邪术师已接入戏法、已知契约法术、宗主、祈唤与契约恩赐检查点。
- 独立法术步骤由 `SpellcastingConfig` 驱动，当前支持 `prepared`、`known`、`spellbook` 与 `pact` 四种模式；角色卡法术页签按环级分组完整展示已选法术（戏法、已准备/已掌握各环），法师额外展示法术书区块并标注"在书中/已准备"状态，计数复用规则层纯函数。
- 角色卡候选法术：`prepared`（牧师/德鲁伊/圣武士）与法师展示"可选未选"候选池（`rules/spellcasting.ts` 的 `getSpellCandidates` 派生）；角色卡可直接点击"准备/取消准备"切换已准备列表（受准备上限约束、满额禁用），写回 `SpellSelections`；`known`/`pact` 职业无候选（2014 规则平时不可更换）。
- 法术效果展开：`SpellRule` 新增 `description` 原创中文效果摘要字段，**全部 489 条法术（`basic-rules-2014` 361 + `xgte-2017-index` 95 + `tcoe-2020-index` 33）均已登记原创摘要**（施法时间/射程/持续时间/效果要点，版权合规转述）；**成长标注已全量补齐**：23 条伤害性戏法含 5/11/17 级成长标注（雷鸣刃/绿焰刃为 TCoE“5 级起”格式），128 条有高环施放条款的法术含“每高 1 环 +X”标注（含非伤害类目标/时长成长顺带项），测试 `spell-descriptions-2014.test.ts` 以双向断言防漏标/误标；**名称译名规范化已完成**：23 条法术名对齐官方简体中文术语与社区主流译名（烙印斩击/死云术/圣焰击/电爪/繁彩球/远距传讯等，含 Sending 与 Message 去重）；**v1.1.5 法术译名全面对齐 5e 不全书（CHM v2025.11.8）**：273 条按不全书译名更新（PHB 173、XGtE 71、TCoE 19、FTD 6、SCC 4，用户指定例外），稳定 ID 与英文名不变，草稿兼容，旧名无残留；`test/rules/name-uniqueness-2014.test.ts` 新增法术/特性/装备名称唯一性测试；新增通用可展开卡片 `components/ui/ExpandableOptionCard.vue`（右侧箭头按钮展开/收起、双击卡片展开并抑制选择、单击主体=选择、250ms 双击判定、`aria-expanded`/`aria-controls` 键盘可达、操作位 div 包裹 + 按钮固定高度）；**展开视觉即 `ExpandableOptionCard` 设计语言**（grid+overflow 容器、透明主按钮 + `›` 窄箭头、title-line 徽标位、单行省略摘要、dashed 分隔 + 与卡片同色展开区 + 标题行）；车卡施法步骤（戏法/法术书/按环分组）与角色卡法术页签（已选/法术书/候选池全部区块）的法术条目均接入展开；车卡职业步骤（第 3 步）的职业卡片同样复用 `ExpandableOptionCard` 展示"职业成长"速览展开（原页面私有组件 `ClassOptionCard` 已删除，职业页展开视觉与法术/种族/装备完全一致）。
- 13 个职业的 2014 起始装备分支、13 个背景固定装备与正式变体替换已接入；冒险套组会展开为具体物品和数量。
- 118 项子职已登记（116 项玩家可用、2 项 DM 专用）；情境效果使用 `selectable`，缺少必选结构的条目才使用 `index-only`。
- 子职特性界面：时间线选中子职后展示其等级特性列表；`requiresChoice` 特性（图腾/猎人/战技等）以 `subclass-feature` 检查点提供选择并持久化；角色卡"能力"页签新增"子职特性"区块；首批可核验派生（龙族血脉 AC/HP）已接入 `derive`。
- 2014 草稿已升级为 schema v7，使用 `enabledSourceIds`、独立灌注绑定和 `CharacterManualEdits`；v2—v6 自动迁移，旧版本会推导实际引用来源并保留未知旧 ID。
- 草稿保存、刷新恢复、JSON 导入导出、规则校验、依赖失效保留和角色卡预览已经接通。
- 角色形象（头像/立绘，可选）：身份步骤与角色卡编辑模式可上传 JPEG/PNG/WebP，自动压缩为 WebP；头像 512×512 圆形裁切并支持焦点调整，立绘最长边缩放至 1600 并支持拖动/滑块调整显示焦点，可从立绘一键生成头像；图片 Blob 存入 IndexedDB（`services/character-media-storage.ts`），草稿 schema v7 只保存媒体引用与焦点元数据；头像/立绘展示在车卡首页草稿列表、角色卡与跑团助手头部，缺失或加载失败时自动隐藏且不显示占位图；删除草稿时同步清理本地图片。
- 完整角色包（`services/character-package.ts`）：ZIP 内含 `character.json` 与可选 `media/avatar.webp`、`media/portrait.webp`，上限 25 MB，导入时校验文件完整性并重建角色与媒体 ID；车卡首页与跑团助手均可导入 ZIP；普通 JSON 导出明确移除媒体引用。
- 角色卡导出：PDF、XLSX 与 JSON 共用角色导出模型；扩展专长、子职选择、工匠灌注、人工编辑和人工法术保持一致，来源关闭的派生不会静默进入导出。JSON 使用 schema v7（不含图片），PDF/XLSX 不写入图片，ZIP 完整角色包包含角色数据与头像/立绘。
- 车卡编辑流程与最终角色卡顶部可直接返回草稿首页；退出只关闭当前活动草稿，保留全部内容和当前步骤，之后可继续编辑。
- 首页角色条第三段信息分级展示：完成态显示职业名；进行中已选职业显示"职业 · 第N步"；未选职业显示"第N步 · 步骤名"（步骤文案来自公共 `STEP_META`，不再裸显 `currentStep` 英文 ID）。
- 主页站点信息：`src/config/site.ts` 为唯一读取 `import.meta.env` 的模块（`VITE_AUTHOR_NAME`/`VITE_GITHUB_URL`/`VITE_AUTHOR_TAGLINE`/`VITE_APP_VERSION`，可选）；首页 hero 底部渲染署名行"由 X 制作 · GitHub · v版本"，全部未配置时隐藏；模板见 `app/.env.example`。
- Docker 部署：`docker-compose.yml` 将根 `.env` 的 `VITE_*` 经 `build.args` 传入，`app/Dockerfile` builder 阶段 ARG/ENV 注入 `pnpm build`（Vite 构建期替换，nginx 运行时无需变量）；根 `.env.example` 为部署模板。
- 角色完成后支持升级/降级与重新编辑：角色卡提供入口（草稿首页角色条仅保留打开与删除，避免拥挤），等级调整弹窗支持 1—20 任意跳级并实时预览影响摘要（新增待补 / 将失效 / 需复查）；确认后升级自动跳转时间线补全新增检查点，降级作废超限选择（保留记录并标记失效）并给出针对性复查清单（熟练加值、属性提升/专长次数、战技数量、已知法术上限、子职特性、生命值上限）；存在失效或未完成检查点时角色卡显示"待补全"徽标；"重新编辑"一键进入车卡流程并智能定位到需处理步骤（否则进入属性步骤）。
- 种族与装备效果展开：`RaceRule` 新增 `description` 原创中文介绍（19 条种族/子种族全部登记，覆盖体型/速度/感官/语言/特性）；`EquipmentRule` 新增 `damageDice`/`damageType`（37 条武器登记伤害骰/类型/特性），全部 136 条装备 description 补齐（护甲 AC/力量需求/隐蔽劣势、工具用品用途）；车卡起源步骤（种族/子种族）、起始装备步骤（分组选项展开装备清单 + pick 行展开详情，`−/＋` 数量控件保留在操作位）与角色卡物品页签（条目化展开）均复用 `ExpandableOptionCard` 展开样式，与法术展开体验一致；背景不展开（另行立项）。
- HP、AC、先攻、熟练加值和攻击加值由纯函数派生并保留来源。

商业规则效果尚未核验的专长和战技保持 `index-only`。它们可用于预览，但会产生资料完整性警告。

## 关键目录

```text
src/components/ui                    通用组件
src/features/quick-build/components  车卡骨架、角色抽屉与操作栏
src/views/character-builder          页面装配、步骤组件和页面流程 hook
src/rules                            2014 注册表、属性、时间线、派生、校验与依赖影响
src/stores                           原始草稿选择和流程动作
src/services                         localStorage 与 JSON 边界
test                                 规则、服务、Store 与组件测试
```

## 尚未完成

1. 牧师、德鲁伊、吟游诗人和术士的完整施法数据与职业检查点。
2. 邪术师高等级秘法奥秘与完整祈唤数量增长。
3. 其余4个职业的完整1—20级检查点。
4. 完整五页签角色卡、资源追踪与角色抽屉全展开态（角色卡导出 PDF/XLSX 已接通模板填充，五页签版面完善仍属后续批次）。

每一批均需通过类型检查、自动测试、生产构建及 375/390/430 px 浏览器验收。
