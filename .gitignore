node_modules
dist
# vite-ssg 的 SSR 中间产物。必须忽略：它只在构建过程中存在，
# 若被当成未跟踪文件，会让构建期算出的构建标识（见 app/scripts/app-build-id.ts）
# 在同一次构建的前后两次求值中得到不同结果。
.vite-ssg-temp
.DS_Store
.vscode
.idea
*.local
.env
.env.*
!.env.example
.reasonix
tmp/**
__pycache__/
*.pyc

*.py
scripts/*.py
scripts/*.txt
app/scripts/*.py
app/scripts/*.txt

# 本地打赏收款码：仅由部署环境提供，不进入仓库
app/public/local-assets/tips/

.chm_output